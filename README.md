# Epidemic-Simulator
A C program that simulates the spread of a disease across a surface with people. The program uses a sequential and parallel approach, to compare the speedup effect of the parallel computation.
